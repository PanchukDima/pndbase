#ifndef MODEL_PREPARATE_H
#define MODEL_PREPARATE_H

#include <QObject>

class model_preparate : public QObject
{
    Q_OBJECT
public:
    explicit model_preparate(QObject *parent = 0);

signals:

public slots:
};

#endif // MODEL_PREPARATE_H